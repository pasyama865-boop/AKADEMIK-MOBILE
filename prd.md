tolong bantu saya memperbaiki dan apa penyebab. tolong berikan saya langkah langkah yang jelas, detail, dan rinci di setiap langkah-nya untuk memperbaiki.

Project path_provider_android: Could not resolve all dependencies for configuration ':debugCompileClasspath'. Configuration `:debugRuntimeClasspath` contains AndroidX dependencies, but the `android.useAndroidX` property is not enabled, which may cause runtime issues.: Could not resolve all dependencies for configuration ':debugCompileClasspath'. Configuration `:debugRuntimeClasspath` contains AndroidX dependencies, but the `android.useAndroidX` property is not enabled, which may cause runtime issues. Set `android.useAndroidX=true` in the `gradle.properties` file and retry. The following AndroidX dependencies are detected: :debugRuntimeClasspath -> androidx.annotation:annotation:1.9.1 :debugRuntimeClasspath -> androidx.annotation:annotation:1.9.1 -> androidx.annotation:annotation-jvm:1.9.1